name: Volga reliable v5.4 shared dual

on:
  workflow_dispatch:
  push:
    branches:
      - test/volga-reliable-v5
    paths:
      - 'tools/volga_ordered_batch_v4b.py'
      - 'tools/volga_ordered_batch_v4c.py'
      - 'tools/volga_batch_trace_v4d.py'
      - 'tools/volga_bounded_reorder_v4e.py'
      - 'tools/volga_reliable_v5.py'
      - 'tools/volga_reliable_v5_impl.go.txt'
      - 'tools/volga_reliable_v5_test.go.txt'
      - 'tools/volga_reliable_v5_ci_compat.py'
      - 'tools/volga_reliable_v5_1_congestion.py'
      - 'tools/volga_reliable_v5_2_resume.py'
      - 'tools/volga_reliable_v5_3_dual.py'
      - 'tools/volga_dual_v5_3.go.txt'
      - 'tools/volga_dual_v5_3_test.go.txt'
      - 'tools/volga_reliable_v5_4_shared_dual.py'
      - 'tools/volga_dual_v5_4.go.txt'
      - 'tools/volga_dual_v5_4_test.go.txt'
      - '.github/workflows/volga-reliable-v5.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.4'

      - name: Apply V1 central batcher
        run: python3 tools/volga_central_batcher_v1.py
      - name: Apply V2 telemetry and redaction
        run: python3 tools/volga_central_batcher_v2_telemetry.py
      - name: Apply V3 byte cap
        run: python3 tools/volga_central_batcher_v3_bytecap.py
      - name: Apply V4 packet telemetry
        run: python3 tools/volga_central_batcher_v4_packet_telemetry.py
      - name: Apply V5 serial worker baseline step
        run: python3 tools/volga_central_batcher_v5_serial_worker.py
      - name: Apply V6 64-worker baseline step
        run: python3 tools/volga_central_batcher_v6_64_workers.py
      - name: Apply runtime benchmark harness
        run: python3 tools/volga_benchmark_harness.py
      - name: Apply body fragmentation v1
        run: python3 tools/volga_body_fragment_v1.py
      - name: Apply adaptive fragmentation v2
        run: python3 tools/volga_adaptive_fragment_v2.py
      - name: Apply HTTP shards v3
        run: python3 tools/volga_http_shards_v3.py
      - name: Apply ordered-batch v4b
        run: python3 tools/volga_ordered_batch_v4b.py
      - name: Apply strict ordered receive v4c
        run: python3 tools/volga_ordered_batch_v4c.py
      - name: Apply per-batch forensic trace v4d
        run: python3 tools/volga_batch_trace_v4d.py
      - name: Apply bounded reorder timer v4e
        run: python3 tools/volga_bounded_reorder_v4e.py
      - name: Apply reliable transport v5
        run: python3 tools/volga_reliable_v5.py
      - name: Apply V5.1 congestion control
        run: python3 tools/volga_reliable_v5_1_congestion.py
      - name: Apply V5.2 receiver resume floor
        run: python3 tools/volga_reliable_v5_2_resume.py
      - name: Apply V5.3.1 dual warm carrier
        run: python3 tools/volga_reliable_v5_3_dual.py
      - name: Apply V5.4 shared reliability dual carrier
        run: python3 tools/volga_reliable_v5_4_shared_dual.py
      - name: Isolate superseded V4 receive tests
        run: python3 tools/volga_reliable_v5_ci_compat.py

      - name: Format generated Go
        run: |
          gofmt -w \
            main.go \
            transport/transport.go \
            transport/yandex/vyandex.go \
            transport/yandex/vyandex_dual_v5_3.go \
            transport/yandex/vyandex_dual_v5_3_test.go \
            transport/yandex/volga_fragment_test.go \
            transport/yandex/volga_http_shards_test.go \
            transport/yandex/volga_ordered_batch_v4b_test.go \
            transport/yandex/volga_ordered_batch_v4c_test.go \
            transport/yandex/volga_bounded_reorder_v4e_test.go \
            transport/yandex/volga_reliable_v5.go \
            transport/yandex/volga_reliable_v5_test.go \
            transport/yandex/volga_reliable_v5_1_test.go \
            transport/yandex/volga_reliable_v5_2_test.go

      - name: Test
        run: go test -timeout 2m ./...

      - name: Build linux amd64
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
            go build -buildvcs=false -trimpath -o openflux-volga-reliable-v5-4-shared-dual .

      - name: Verify runtime knobs
        run: |
          ./openflux-volga-reliable-v5-4-shared-dual -h 2>&1 | grep -E -- 'volga-workers|volga-http-shards|volga-batch-bytes|volga-http-body-limit|volga-normalize-mtu|volga-url-secondary|volga-rotate-seconds' >/dev/null

      - name: Verify V5.4 shared reliability source
        run: |
          python3 - <<'PY'
          import re
          from pathlib import Path

          main = Path('main.go').read_text()
          dual = Path('transport/yandex/vyandex_dual_v5_3.go').read_text()
          wire = Path('transport/yandex/vyandex.go').read_text()
          rel = Path('transport/yandex/volga_reliable_v5.go').read_text()

          checks = {
              'dual constructor CLI': (main, r'NewDualYandexVolgaTransport\s*\('),
              'secondary URL flag': (main, r'volga-url-secondary'),
              'rotation flag': (main, r'volga-rotate-seconds'),
              'dual type': (dual, r'type\s+DualYandexVolgaTransport\s+struct'),
              'one logical send queue': (dual, r'return\s+d\.primary\.Send\(data\)'),
              'wire selector installed': (dual, r'd\.primary\.volgaReliableWireSelector\s*=\s*d\.activeWireRelay'),
              'secondary receive owner': (dual, r'd\.secondary\.volgaReliableOwner\s*=\s*d\.primary\.ws'),
              'standby app keepalive disabled': (dual, r'stopDualApplicationKeepAlive'),
              'idle reset': (dual, r'idle reset after='),
              'shared telemetry': (dual, r'reliability=shared'),
              'wire target helper': (wire, r'func\s+\(r \*relayClient\)\s+volgaReliableWireTarget'),
              'wire post selection': (wire, r'wire := r\.volgaReliableWireTarget\(\)'),
              'shared receive delegation': (rel, r'w\.volgaReliableOwner\.handleVolgaReliableRecords'),
              'shared recv state': (rel, r'return w\.volgaReliableOwner\.volgaReliableRecvState\(\)'),
              'V5.2 replay floor': (rel, r'volgaReliableReplayFloor'),
              'V5.2 receiver baseline': (rel, r'receiver baseline session='),
          }
          for name, (text, pattern) in checks.items():
              if not re.search(pattern, text):
                  raise SystemExit(f'missing V5.4 invariant: {name}')

          # The logical session metadata must stay on the owner relay even when
          # the actual Yandex POST is routed through carrier B.
          post_start = wire.index('func (r *relayClient) postBatchV5')
          post_end = wire.index('func (r *relayClient) volgaReliableWireTarget', post_start)
          post = wire[post_start:post_end]
          if 'r.volgaReliableSession()' not in post:
              raise SystemExit('V5.4 logical session no longer owned by primary relay')
          if 'wire.auth' not in post or 'wire.httpClient' not in post:
              raise SystemExit('V5.4 Yandex POST is not routed through selected wire carrier')

          const_checks = {
              'replay cap': r'volgaReliableReplayCap\s*=\s*16384\b',
              'retry concurrency': r'volgaReliableRetryConcurrency\s*=\s*8\b',
              'fast retry minimum': r'volgaReliableFastRetryMin\s*=\s*180\s*\*\s*time\.Millisecond',
              'fast retry burst': r'volgaReliableFastRetryBurst\s*=\s*8\b',
              'timeout retry burst': r'volgaReliableTimeoutRetryBurst\s*=\s*8\b',
          }
          for name, pattern in const_checks.items():
              if not re.search(pattern, rel):
                  raise SystemExit(f'missing V5.4 reliability invariant: {name}')
          PY

      - name: Verify V5 wire isolation from V4 ordered receive
        run: |
          python3 - <<'PY'
          from pathlib import Path
          s = Path('transport/yandex/vyandex.go').read_text()
          a = s.index('func (w *wsListener) handleRelayMessage')
          b = s.index('func (w *wsListener) deliverOrderedBundle', a)
          live = s[a:b]
          assert 'handleOrderedBundle' not in live
          assert 'handleBundleItem' in live
          PY

      - name: Checksums and metadata
        run: |
          sha256sum openflux-volga-reliable-v5-4-shared-dual > SHA256SUMS
          {
            echo "source_branch=test/volga-reliable-v5"
            echo "workflow_commit=${GITHUB_SHA}"
            echo "base=v5.3.1-clean-warm-standby"
            echo "experiment=v5.4-shared-reliability-dual-carrier"
            echo "dual_mode=active-standby-no-striping"
            echo "dual_rotation_default_seconds=25"
            echo "dual_idle_reset_seconds=2x-rotation"
            echo "dual_application_keepalive=disabled"
            echo "dual_receive=both-carriers-one-shared-state"
            echo "dual_reliability=single-v5.2-owner"
            echo "dual_retransmit=current-active-carrier"
            echo "dual_standby=wire-silent"
            echo "workers_per_carrier=runtime-configured"
            echo "reliability=payload-session-seq+replay-floor+cumulative-ack+sack-ranges+selective-retransmit+dedupe"
            echo "retry_concurrency=8"
            echo "fast_retry_min_ms=180"
            echo "fast_retry_burst=8"
            echo "timeout_retry_burst=8"
            echo "http_shards_default=1"
            echo "batch_bytes_baseline=5000"
            echo "http_body_limit=8000"
            echo "go_version=$(go version)"
          } > BUILD-METADATA

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: openflux-volga-reliable-v5-4-shared-dual-${{ github.sha }}
          path: |
            openflux-volga-reliable-v5-4-shared-dual
            SHA256SUMS
            BUILD-METADATA
          retention-days: 7
